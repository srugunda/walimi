<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationdrawermateriallibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_w_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_w_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_Tabs"></activity>
        <activity android:name=".FarmerCategoryActivity" />
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CartActivity" />
        <activity android:name=".DisplayItemsActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>